/**
 * nodejs 中常用全局api
 * http://nodejs.cn/api/globals.html
 */


/**
 * 1.模块化
 * 
 * module
 * exports
 * require()
 * 
 */

/**
 * 2. Buffer 类
 */


/**
 * 3.  URL
 * http中用到的
 */

/**
 * 4. 特殊的全局对象,也可以说是每个模块中有的
 * 
 * __dirname
    __filename
 * exports
    module
    require()
 */

    /**
     * 定时器
     * 
setImmediate(callback[, ...args])
setInterval(callback, delay[, ...args])
setTimeout(callback, delay[, ...args])
process.nextTick()
     */